language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t kwangwooDocker/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true kwangwooDocker/react-test-app npm run test

after_success:
  - docker build -t kwangwooDocker/docker-frontend ./frontend
  - docker build -t kwangwooDocker/docker-backend ./backend
  - docker build -t kwangwooDocker/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push kwangwooDocker/docker-frontend
  - docker push kwangwooDocker/docker-backend
  - docker push kwangwooDocker/docker-nginx
# deploy:
#   provider: elasticbeanstalk
#   region: "ap-northeast-2"
#   app: "docker-fullstack-app"
#   env: "DockerFullstackApp-env"
#   bucket_name: elasticbeanstalk-ap-northeast-2-972153559337
#   bucket_path: "docker-fullstack-app"
#   on:
#     branch: master

#   access_key_id: $AWS_ACCESS_KEY
#   secret_access_key: $AWS_SECRET_ACCESS_KEY
